%=======================================================================
%
%     readln
%
%=======================================================================

      function [line] = readln(fid,nl);

      for is=1:nl
        tmp = abs(fgetl(fid));
        s(is,2:size(tmp,2)+1) = tmp;
      end
      line = s(:,2:size(s,2));

%=======================================================================
